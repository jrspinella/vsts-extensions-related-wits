{
  "name": "related-workitems",
  "license": "MIT",
  "description": "Easier way to manage bug bashes",
  "private": true,
  "main": "webpack.config.js",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist *.vsix",
    "clean:all": "npm run clean && rimraf typings node_modules",
    "build": "npm run clean && mkdir dist && webpack --display-error-details --progress --colors --output-path ./dist -p",
    "dev": "webpack-dev-server -d --hot --progress --colors --content-base ./ --port 8888",
    "dev:ssl": "webpack-dev-server -d --hot --progress --colors --https --content-base ./ --port 8888",    
    "package:dev": "node ./configs/packageDev",
    "package:devssl": "node ./configs/packageDevSSL",
    "package:release": "npm run build && node ./configs/packageRelease",    
    "postinstall": "typings install"
  },
  "devDependencies": {
    "@types/react": "^15.0.22",
    "@types/react-dom": "^15.5.0",
    "@types/jquery": "^2.0.41",
    "webpack": "^2.4.1",
    "webpack-dev-server": "^2.4.2",
    "rimraf": "^2.6.1",
    "sass-loader": "^6.0.3",
    "style-loader": "^0.16.1",
    "css-loader": "^0.28.0",
    "ts-loader": "^2.0.3",
    "node-sass": "^4.5.2",
    "tfx-cli": "^0.4.5",
    "typescript": "^2.2.2",
    "typings": "^2.1.1",
    "uglifyjs-webpack-plugin": "^0.4.3",
    "copy-webpack-plugin": "^4.0.1"
  },
  "dependencies": {
    "vss-web-extension-sdk": "^2.115.0",
    "es6-promise": "^4.1.0",
    "office-ui-fabric-react": "^2.21.0",
    "vsts-extension-react-widgets": "0.0.4"
  }
}